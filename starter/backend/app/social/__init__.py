"""Connecting the social accounts a user already owns.

This package connects accounts. It does not publish, schedule or read timelines,
and there is deliberately no code path here that writes to a network.
"""
