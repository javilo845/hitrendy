fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
sqlalchemy>=2.0,<3
alembic>=1.13,<2
psycopg[binary]>=3.2,<4
redis>=5,<7
httpx>=0.27,<1
PyJWT[crypto]>=2.10,<3
# Direct dependency since WAVE-012: AES-GCM for provider tokens at rest. It used
# to arrive only through PyJWT's extra, which would have made the encryption of
# stored credentials hostage to an unrelated package's optional dependencies.
cryptography>=43,<50
pytest>=8,<9
pytest-asyncio>=0.24,<1
aiosqlite>=0.20,<1
python-multipart>=0.0.9,<1
boto3>=1.35,<2
pillow>=10.4,<12
defusedxml>=0.7,<1
# Direct dependency: app.core.config loads the repository-root .env on import so
# every entry point (uvicorn, alembic) sees the same configuration.
python-dotenv>=1.0,<2
